# Supabase project (see supabase/ for schema + migrations)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only, currently unused by the app — reserved for future
# admin-only flows that need to bypass Row Level Security.
SUPABASE_SECRET_KEY=
